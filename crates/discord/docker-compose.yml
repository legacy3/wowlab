# Copy .env.example to .env and set DISCORD_TOKEN, or uncomment below.

services:
  bot:
    image: ghcr.io/legacy3/wowlab-discord:latest
    restart: unless-stopped
    env_file: .env
    environment:
      RUST_LOG: wowlab_discord=info
      # DISCORD_TOKEN: your-bot-token
