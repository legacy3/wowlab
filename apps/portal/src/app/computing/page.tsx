import { ComputingContent } from "@/components/computing/computing-content";

export const metadata = {
  title: "Computing | WoW Lab",
  description:
    "View worker pool status, simulation history, and system diagnostics",
};

export default function ComputingPage() {
  return <ComputingContent />;
}
